server:
  port: 3702
spring:
  jpa: 
    hibernate:
      ddl-auto: update
      use-new-id-generator-mappings: true
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5433/mangapp
    username: postgres
    password: admin